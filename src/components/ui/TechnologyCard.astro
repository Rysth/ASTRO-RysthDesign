---
// Props para el componente TechnologyCard
const { src, alt, customClass = "" } = Astro.props;
import { Image } from "astro:assets";
---

<picture
  class="grid w-20 h-20 transition duration-300 place-items-center group rounded-xl"
>
  <Image
    src={src}
    alt={alt}
    class={`w-14 transition duration-300 opacity-50 group-hover:grayscale-0 group-hover:opacity-100 ${customClass}`}
  />
</picture>
